import React, {useState} from "react";
    
export default function SuppButtonFunction() {

    // const removeCard = (id) => {
    //     setCards(cards.filter((card) => card.id !== id));
    // };
}
    
    